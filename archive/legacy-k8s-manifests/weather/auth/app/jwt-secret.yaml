# Example only — do not commit real secrets. In practice this value is
# injected at deploy time, never checked into version control. Must be
# identical to the Secret used by the ui deployment.
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
data:
  jwt-secret: Q0hBTkdFTUU=  # base64("CHANGEME")
