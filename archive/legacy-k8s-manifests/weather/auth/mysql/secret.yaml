# Example only — do not commit real secrets. In practice this value is
# injected at deploy time, never checked into version control.
apiVersion: v1
data:
  my-sql-root-password: Q0hBTkdFTUU=  # base64("CHANGEME")
kind: Secret
metadata:
  name: mysql-secret
