# Example only — do not commit real secrets. In practice this value is
# injected at deploy time (e.g. `kubectl create secret` or CI `--set`),
# never checked into version control.
apiVersion: v1
data:
  api-key: Q0hBTkdFTUU=  # base64("CHANGEME")
kind: Secret
metadata:
  name: api-key-secret
  namespace: default
type: Opaque
